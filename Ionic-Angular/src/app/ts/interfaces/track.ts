export interface TrackInterface {
  // id: number;
  title: string;
  // user: Object;
  // createdAt: Date;
  url: string;
}

export interface Track {
  userId: string;
  _id: string;
  title: string;
  createdAt: Date;
  url: string;
}